

# Atualizar META_ADS_TOKEN com novo Access Token

## O que será feito
1. **Atualizar o secret `META_ADS_TOKEN`** com o novo token fornecido
2. **Testar a sync do Meta Ads** chamando a edge function para confirmar que o token funciona

Nenhuma alteração de código necessária — apenas atualização do secret e teste.

